language: cpp

os:
  - osx

compiler:
  - clang

branches:
  except:
    - windows
    - linux

before_script:
  - brew update
  - brew install ninja meson
  - git submodule update --init --recursive
  - mkdir build
  - meson setup build

script:
  - cd build
  - ninja
  - ninja test